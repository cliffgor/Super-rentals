
import { rentalPopullarity } from 'super-rentals/helpers/rental-popullarity';
import { module, test } from 'qunit';

module('Unit | Helper | rental popullarity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rentalPopullarity([42]);
  assert.ok(result);
});

